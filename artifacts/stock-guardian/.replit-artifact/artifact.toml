kind = "web"
previewPath = "/"
title = "Stock Guardian"
version = "1.0.0"
id = "artifacts/stock-guardian"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/stock-guardian run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/stock-guardian", "run", "build" ]
publicDir = "artifacts/stock-guardian/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
